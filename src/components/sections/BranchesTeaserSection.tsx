"use client";
import { useTranslations } from "next-intl"; import { useRouter } from "@/i18n/routing"; import { Button } from "@/components/common/Button"; import { Section, SectionHead } from "@/components/common/Section";
export function BranchesTeaserSection({ branches }: { branches: { id: number; code: string; name: string }[] }) { const t = useTranslations("branchesSection"); const router = useRouter(); return <Section><SectionHead center eyebrow={t("eyebrow")} title={t("title")} sub={t("sub")} /><div className="grid items-center gap-8 md:grid-cols-[1.1fr_.9fr]"><div className="relative min-h-[360px] overflow-hidden rounded-xl bg-brand-grad-soft p-10"><div className="absolute inset-10 rounded-[45%_55%_35%_65%] border-[18px] border-brand/15" /><div className="relative grid h-full min-h-[280px] place-items-center"><span className="grid h-28 w-28 place-items-center rounded-full bg-brand-grad font-display text-4xl font-black text-white shadow-lg">{branches.length}</span></div></div><div className="rounded-lg border border-line bg-surface p-6"><ul className="grid grid-cols-2 gap-3">{branches.map(b => <li key={b.id} className="flex items-center gap-2 rounded-lg bg-surface-2 px-3 py-2.5 text-sm font-semibold"><span className="text-teal">●</span>{b.code} · {b.name}</li>)}</ul>{!branches.length ? <p className="text-center text-muted">Đang cập nhật hệ thống cơ sở.</p> : null}<Button variant="primary" className="mt-6 w-full" onClick={() => router.push("/branches")}>{t("cta")} →</Button></div></div></Section>; }
