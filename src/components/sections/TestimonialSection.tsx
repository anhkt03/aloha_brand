"use client";
import { useTranslations } from "next-intl"; import { Container } from "@/components/common/Container"; import { SectionHead } from "@/components/common/Section";
type Feedback = { id: number; name: string; rating: number; comment: string };
export function TestimonialSection({ testimonials }: { testimonials: Feedback[] }) { const t = useTranslations("testimonials"); return <section className="section"><Container><SectionHead center eyebrow={t("eyebrow")} title={t("title")} /><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{testimonials.map(item => <blockquote key={item.id} className="card hover"><div className="text-accent">{"★".repeat(item.rating)}</div><p className="mt-4 leading-relaxed text-ink-soft">“{item.comment}”</p><footer className="mt-5 font-display font-extrabold text-brand">{item.name}</footer></blockquote>)}</div>{!testimonials.length ? <div className="card text-center text-muted">Chưa có đánh giá được duyệt.</div> : null}</Container></section>; }
