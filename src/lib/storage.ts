import "server-only";
import { randomUUID } from "crypto";
import { createClient } from "@supabase/supabase-js";
const BUCKET = "aloha-media"; const allowed = new Set(["image/jpeg", "image/png", "image/webp", "image/avif", "image/svg+xml"]); const extensions: Record<string, string> = { "image/jpeg": "jpg", "image/png": "png", "image/webp": "webp", "image/avif": "avif", "image/svg+xml": "svg" };
function adminClient() { const url = process.env.NEXT_PUBLIC_SUPABASE_URL; const key = process.env.SUPABASE_SECRET_KEY; if (!url || !key) throw new Error("Supabase Storage chưa được cấu hình."); return createClient(url, key, { auth: { persistSession: false, autoRefreshToken: false } }); }
export async function uploadImage(file: File, folder: string) { if (!allowed.has(file.type)) throw new Error("Định dạng ảnh không được hỗ trợ."); if (file.size > 5 * 1024 * 1024) throw new Error("Ảnh vượt quá giới hạn 5MB."); if (!/^(course-types|courses|news|branches)\/\d+(\/(cover|gallery))?$/.test(folder)) throw new Error("Đường dẫn upload không hợp lệ."); const path = `${folder}/${randomUUID()}.${extensions[file.type]}`; const supabase = adminClient(); const { error } = await supabase.storage.from(BUCKET).upload(path, file, { contentType: file.type, upsert: false }); if (error) throw error; return { path, url: supabase.storage.from(BUCKET).getPublicUrl(path).data.publicUrl }; }
export async function deleteImage(path: string) { if (path.includes("..") || path.startsWith("/")) throw new Error("Đường dẫn ảnh không hợp lệ."); const { error } = await adminClient().storage.from(BUCKET).remove([path]); if (error) throw error; }
