import { unstable_cache } from "next/cache";
import { prisma } from "@/lib/prisma";
function pick<T extends { locale: string }>(rows: T[], locale: string) { return rows.find(x => x.locale === locale) ?? rows.find(x => x.locale === "vi") ?? rows[0]; }
export async function getPublicCourses(locale: string) { const rows = await unstable_cache(() => prisma.course.findMany({ where: { status: { in: ["OPEN", "COMING_SOON"] }, courseType: { active: true }, courseLevel: { active: true } }, include: { translations: true, courseType: { include: { translations: true } }, courseLevel: { include: { translations: true } } }, orderBy: [{ sortOrder: "asc" }, { id: "asc" }] }), ["public-courses"], { tags: ["courses"], revalidate: 300 })(); return rows.map(x => { const t = pick(x.translations, locale); const type = pick(x.courseType.translations, locale); const level = pick(x.courseLevel.translations, locale); return { id: x.id, slug: x.slug, title: t?.title ?? x.slug, label: t?.label ?? "", target: t?.target ?? "", outcome: t?.outcome ?? "", syllabus: t?.syllabus ?? [], roadmap: t?.roadmap ?? null, type: type?.name ?? "", level: level?.name ?? "", durationMonths: x.durationMonths, totalSessions: x.totalSessions, status: x.status, priceVnd: x.priceVnd, discountPercent: x.discountPercent, iconUrl: x.iconUrl }; }); }
export async function getPublicNews(locale: string) { const rows = await unstable_cache(() => prisma.newsArticle.findMany({ where: { status: "PUBLISHED", publishedAt: { lte: new Date() }, category: { active: true } }, include: { translations: true, category: { include: { translations: true } } }, orderBy: [{ prominent: "desc" }, { publishedAt: "desc" }] }), ["public-news"], { tags: ["news"], revalidate: 300 })(); return rows.map(x => { const t = pick(x.translations, locale); return { id: x.id, slug: x.slug, title: t?.title ?? x.slug, excerpt: t?.excerpt ?? "", content: t?.content ?? "", coverImage: x.coverImage, gallery: x.gallery, category: pick(x.category.translations, locale)?.name ?? "", author: x.author, prominent: x.prominent, publishedAt: x.publishedAt.toISOString() }; }); }
export async function getPublicNewsBySlug(locale: string, slug: string) { return (await getPublicNews(locale)).find(x => x.slug === slug) ?? null; }
export const getPublicFeedback = unstable_cache(() => prisma.feedback.findMany({ where: { active: true }, select: { id: true, name: true, rating: true, comment: true }, orderBy: { createdAt: "desc" }, take: 20 }), ["public-feedback"], { tags: ["feedback"], revalidate: 300 });
export const getPublicBranches = unstable_cache(() => prisma.branch.findMany({ where: { active: true }, select: { id: true, code: true, name: true, address: true, phone: true, mapUrl: true, lat: true, lng: true }, orderBy: { code: "asc" } }), ["public-branches"], { tags: ["branches"], revalidate: 300 });
