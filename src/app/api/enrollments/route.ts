import { NextRequest, NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { enrollmentSchema } from "@/lib/validation/enrollment";
const buckets = new Map<string, { count: number; reset: number }>();
export async function GET() { const courses = await prisma.course.findMany({ where: { status: "OPEN" }, select: { id: true, translations: { where: { locale: "vi" }, select: { title: true } } }, orderBy: { sortOrder: "asc" } }); return NextResponse.json(courses.map(x => ({ id: x.id, title: x.translations[0]?.title ?? `Khóa học ${x.id}` }))); }
export async function POST(request: NextRequest) { const ip = request.headers.get("x-forwarded-for")?.split(",")[0] ?? "local"; const now = Date.now(); const bucket = buckets.get(ip); if (bucket && bucket.reset > now && bucket.count >= 5) return NextResponse.json({ message: "Bạn gửi quá nhanh. Vui lòng thử lại sau." }, { status: 429 }); buckets.set(ip, bucket && bucket.reset > now ? { ...bucket, count: bucket.count + 1 } : { count: 1, reset: now + 60_000 }); const body = await request.json().catch(() => null); const parsed = enrollmentSchema.safeParse(body); if (!parsed.success) return NextResponse.json({ message: "Thông tin đăng ký không hợp lệ.", fieldErrors: parsed.error.flatten().fieldErrors }, { status: 400 }); const data = parsed.data; const exists = await prisma.enrollment.findFirst({ where: { phone: data.phone, courseId: data.courseId, createdAt: { gte: new Date(now - 120_000) } }, select: { id: true } }); if (exists) return NextResponse.json({ message: "Đăng ký đã được ghi nhận." }, { status: 200 }); const course = await prisma.course.findFirst({ where: { id: data.courseId, status: "OPEN" }, select: { id: true } }); if (!course) return NextResponse.json({ message: "Khóa học không còn nhận đăng ký." }, { status: 400 }); await prisma.enrollment.create({ data: { ...data, email: data.email || null } }); return NextResponse.json({ message: "Đăng ký thành công." }, { status: 201 }); }
