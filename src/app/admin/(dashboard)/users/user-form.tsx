import { UserRole } from "@prisma/client";
import { SubmitButton } from "@/components/admin/submit-button";
type User = { id: number; username: string; name: string; email: string | null; role: UserRole; active: boolean };
export function UserForm({ action, item }: { action: (data: FormData) => void | Promise<void>; item?: User }) { return <form action={action} className="grid max-w-xl gap-4"><input type="hidden" name="id" value={item?.id ?? ""} /><label>Tên đăng nhập<input name="username" required defaultValue={item?.username} className="mt-1 w-full rounded-xl border border-line p-3" /></label><label>Họ tên<input name="name" required defaultValue={item?.name} className="mt-1 w-full rounded-xl border border-line p-3" /></label><label>Email<input name="email" type="email" defaultValue={item?.email ?? ""} className="mt-1 w-full rounded-xl border border-line p-3" /></label><label>Vai trò<select name="role" defaultValue={item?.role ?? "STAFF"} className="mt-1 w-full rounded-xl border border-line p-3">{Object.values(UserRole).map(x => <option key={x}>{x}</option>)}</select></label><label>{item ? "Mật khẩu mới (để trống nếu giữ nguyên)" : "Mật khẩu"}<input name="password" type="password" required={!item} className="mt-1 w-full rounded-xl border border-line p-3" /></label><label><input type="checkbox" name="active" defaultChecked={item?.active ?? true} /> Tài khoản hoạt động</label><SubmitButton pendingText="Đang lưu..." className="btn btn-primary justify-self-start">Lưu tài khoản</SubmitButton></form>; }
