import { notFound } from "next/navigation"; import { prisma } from "@/lib/prisma"; import { requireRole } from "@/lib/auth"; import { UserForm } from "../user-form"; import { saveUser } from "../actions";
export default async function UserPage({ params }: { params: Promise<{ id: string }> }) { await requireRole("ADMIN"); const item = await prisma.user.findUnique({ where: { id: Number((await params).id) }, select: { id: true, username: true, name: true, email: true, role: true, active: true } }); if (!item) notFound(); return <><h1 className="mb-6 text-3xl font-black">Sửa tài khoản</h1><UserForm action={saveUser} item={item} /></>; }
