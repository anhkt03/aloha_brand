import { SubmitButton } from "@/components/admin/submit-button";
type Feedback = { id: number; name: string; rating: number; comment: string; active: boolean };
export function FeedbackForm({ action, item }: { action: (data: FormData) => void | Promise<void>; item?: Feedback }) { return <form action={action} className="grid max-w-xl gap-4"><input type="hidden" name="id" value={item?.id ?? ""} /><label>Tên<input name="name" required defaultValue={item?.name} className="mt-1 w-full rounded-xl border border-line p-3" /></label><label>Rating<input name="rating" type="number" min="1" max="5" required defaultValue={item?.rating ?? 5} className="mt-1 w-full rounded-xl border border-line p-3" /></label><label>Nội dung<textarea name="comment" required defaultValue={item?.comment} className="mt-1 min-h-32 w-full rounded-xl border border-line p-3" /></label><label><input type="checkbox" name="active" defaultChecked={item?.active} /> Đã duyệt hiển thị</label><SubmitButton pendingText="Đang lưu..." className="btn btn-primary justify-self-start">Lưu</SubmitButton></form>; }
