import { notFound } from "next/navigation"; import { prisma } from "@/lib/prisma"; import { BranchForm } from "../branch-form"; import { deleteBranch, saveBranch } from "../actions";
export default async function BranchPage({ params }: { params: Promise<{ id: string }> }) { const id = Number((await params).id); const item = await prisma.branch.findUnique({ where: { id } }); if (!item) notFound(); return <><h1 className="mb-6 text-3xl font-black">Sửa cơ sở</h1><BranchForm action={saveBranch} item={item} /><form action={deleteBranch.bind(null, id)} className="mt-5"><button className="btn btn-ghost btn-sm text-red-700">Xóa</button></form></>; }
