import { SubmitButton } from "@/components/admin/submit-button";
type Branch = { id: number; code: string; name: string; address: string; phone: string | null; mapUrl: string | null; lat: number | null; lng: number | null; active: boolean };
export function BranchForm({ action, item }: { action: (data: FormData) => void | Promise<void>; item?: Branch }) { return <form action={action} className="grid max-w-2xl gap-4 md:grid-cols-2"><input type="hidden" name="id" value={item?.id ?? ""} />{[["code", "Mã cơ sở", item?.code], ["name", "Tên", item?.name], ["phone", "Điện thoại", item?.phone], ["mapUrl", "Google Maps URL", item?.mapUrl], ["lat", "Vĩ độ", item?.lat], ["lng", "Kinh độ", item?.lng]].map(([name, label, value]) => <label key={String(name)}>{label}<input name={String(name)} required={name === "code" || name === "name"} defaultValue={value ?? ""} className="mt-1 w-full rounded-xl border border-line p-3" /></label>)}<label className="md:col-span-2">Địa chỉ<textarea name="address" required defaultValue={item?.address} className="mt-1 w-full rounded-xl border border-line p-3" /></label><label><input type="checkbox" name="active" defaultChecked={item?.active ?? true} /> Đang hoạt động</label><SubmitButton pendingText="Đang lưu..." className="btn btn-primary justify-self-start md:col-span-2">Lưu cơ sở</SubmitButton></form>; }
