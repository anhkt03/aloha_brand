import { TranslationTabs } from "@/components/admin/translation-tabs";
export function NewsCategoryForm({ action, item }: { action: (formData: FormData) => void | Promise<void>; item?: { id: number; iconUrl: string | null; sortOrder: number; translations: { locale: string; name: string }[] } }) { const values = Object.fromEntries((item?.translations ?? []).map(x => [x.locale, x.name])); return <form action={action} className="grid max-w-xl gap-5"><input type="hidden" name="id" value={item?.id ?? ""} /><label>Icon URL<input name="iconUrl" defaultValue={item?.iconUrl ?? ""} className="mt-1 w-full rounded-xl border border-line p-3" /></label><label>Thứ tự<input type="number" min="0" name="sortOrder" defaultValue={item?.sortOrder ?? 0} className="mt-1 w-full rounded-xl border border-line p-3" /></label><TranslationTabs values={values} /><button className="btn btn-primary justify-self-start">Lưu</button></form>; }
